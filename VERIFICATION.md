# 公开发布前审查与真实运行记录

- 运行资料从仓库内的 `data/styles.json` 读取；服务端不执行数据文件中的 JavaScript。
- 浏览器仅接收界面需要的风格字段；服务器只允许访问明确列出的前端文件和 `data/assets/` 图片，不提供读取源码或完整资料 JSON 的静态路由。
- `.gitignore` 未排除本次要发布的 `data/` 与 `screenshots/`；会排除环境变量文件、本地验证记录、依赖目录、系统文件和 npm 日志。
- 根目录 `LICENSE` 使用标准 MIT 文本，标明适用于项目原创代码和文档；README 与第三方说明区分素材许可，并提示上游权利信息及商用风险由使用者自行核验。
- 已核对 261 条风格资料及 522 张 WebP 图片；资料仅含界面所需的 7 个字段，图片与授权素材副本逐一通过 SHA-256 校验。
- 对待发布文件做了文本与二进制隐私模式扫描，未发现本地目录、机器用户名、文件 URL 或密钥样式字符串；WebP 文件未发现 EXIF、XMP 或 ICC 元数据块。
- API Key 只由服务端进程从环境变量读取；API 错误响应不再把 TypeSafe 原始响应内容转发给浏览器。
- `node --check` 语法检查通过。
- 实际执行 `npm start` 启动本地服务，并在真实浏览器中打开页面；主页显示 261 条风格资料。使用普通主题“下班后的雨夜”执行一次真实搜索，TypeSafe API 返回 8 个候选，页面报告耗时 2.3 秒。服务只确认 API Key 已配置，没有读取或输出 Key 内容。
- 保存了 5 张真实运行页面截图至 `screenshots/`。截图只包含网页内容，不含浏览器地址栏或本地目录；转为 PNG 后逐张检查，PNG 仅包含 IHDR、IDAT、IEND 块，无 EXIF、ICC、XMP 或文本元数据。人工核对画面未见本地路径或凭据。
